---
phase: 03-data-browsing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flask-example/api.py
  - flask-example/views.py
  - flask-example/auth.py
  - flask-example/templates/base.html
  - flask-example/templates/index.html
  - flask-example/templates/accounts.html
  - flask-example/templates/account.html
  - flask-example/templates/location.html
  - flask-example/templates/reviews.html
  - flask-example/templates/_breadcrumb.html
  - flask-example/templates/_error.html
  - flask-example/templates/callback_success.html
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
  - UX-05

must_haves:
  truths:
    - "Authenticated user visiting / sees a list of their Google Business Profile accounts"
    - "Clicking an account navigates to /account/<id> showing locations"
    - "Clicking a location navigates to /location/<id>?account=<aid> showing details and View Reviews button"
    - "Clicking View Reviews navigates to /location/<id>/reviews?account=<aid> showing paginated reviews"
    - "Next page link appears when nextPageToken exists, using ?page_token= in URL"
    - "Empty accounts/locations/reviews show friendly explanatory messages"
    - "API errors (401/403/429/404/5xx) display inline error banners with appropriate messages and links"
    - "Breadcrumb navigation appears on account, location, and reviews pages"
    - "OAuth callback shows success message and auto-redirects to / after 3 seconds"
  artifacts:
    - path: "flask-example/api.py"
      provides: "get_client() and get_reviews_page() helpers"
      exports: ["get_client", "get_reviews_page"]
    - path: "flask-example/views.py"
      provides: "Routes for /, /account/<id>, /location/<id>, /location/<id>/reviews"
      min_lines: 60
    - path: "flask-example/templates/_breadcrumb.html"
      provides: "Reusable breadcrumb partial"
      contains: "aria-label"
    - path: "flask-example/templates/_error.html"
      provides: "Reusable error banner partial"
      contains: "role=\"alert\""
    - path: "flask-example/templates/callback_success.html"
      provides: "Meta refresh success page"
      contains: "meta http-equiv"
  key_links:
    - from: "flask-example/views.py"
      to: "flask-example/api.py"
      via: "get_client() and get_reviews_page() imports"
      pattern: "from api import"
    - from: "flask-example/api.py"
      to: "flask-example/cookies.py"
      via: "decrypt_tokens for credential reconstruction"
      pattern: "decrypt_tokens"
    - from: "flask-example/views.py"
      to: "google_reviews_client exceptions"
      via: "try/except blocks in each route"
      pattern: "except AuthenticationError"
---

<objective>
Wire the GoogleReviewsClient library into Flask routes, creating the full accounts -> locations -> reviews browsing hierarchy with error handling, breadcrumbs, pagination, and empty states.

Purpose: This is the core value delivery of the demo app -- users can actually browse their Google Business Profile data.
Output: Complete data browsing UI with all routes, templates, API helper, error handling, and navigation.
</objective>

<execution_context>
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/flask-demo/ROADMAP.md
@.planning/flask-demo/STATE.md
@.planning/flask-demo/phases/03-data-browsing/03-CONTEXT.md
@.planning/flask-demo/phases/03-data-browsing/03-RESEARCH.md
@.planning/flask-demo/phases/03-data-browsing/03-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/google_reviews_client/client.py:
```python
class GoogleReviewsClient:
    def __init__(self, credentials: google.auth.credentials.Credentials): ...
    def _authenticated_get(self, url: str, params: dict | None = None, extra_headers: dict[str, str] | None = None) -> dict: ...
    def list_accounts(self) -> list[Account]: ...
    def list_locations(self, account: str) -> list[Location]: ...  # account = "accounts/123"
    def list_reviews(self, location: str, ...) -> Iterator[Review]: ...  # DO NOT USE - auto-paginates
```

From src/google_reviews_client/models.py:
```python
@dataclass
class Account:
    name: str           # "accounts/123"
    account_name: str   # display name
    type: str | None

@dataclass
class Location:
    name: str           # "locations/456"
    location_id: str    # "456"
    account_id: str     # "123"
    title: str | None
    store_code: str | None
    @property
    def full_name(self) -> str:  # "accounts/123/locations/456"

@dataclass
class Review:
    review_id: str
    reviewer: Reviewer      # .display_name, .profile_photo_url, .is_anonymous
    star_rating: StarRating # .value -> int (1-5)
    comment: str
    create_time: datetime
    update_time: datetime
    review_reply: ReviewReply | None  # .comment, .update_time
    @property
    def rating_value(self) -> int
    @property
    def has_reply(self) -> bool
```

From src/google_reviews_client/exceptions.py:
```python
class AuthenticationError(GoogleReviewsError): ...   # 401
class GooglePermissionError(GoogleReviewsError): ... # 403
class NotFoundError(GoogleReviewsError): ...         # 404
class RateLimitError(GoogleReviewsError): ...        # 429, has .retry_after
class GoogleAPIError(GoogleReviewsError): ...        # 5xx
```

From src/google_reviews_client/constants.py:
```python
BUSINESS_BASE = "https://mybusiness.googleapis.com/v4"
```

From flask-example/cookies.py:
```python
TOKEN_COOKIE_NAME = "greviews_tokens"
def encrypt_tokens(token_dict: dict, fernet: Fernet) -> str: ...
def decrypt_tokens(cookie_value: str, fernet: Fernet) -> dict | None: ...
```

From flask-example/auth.py:
```python
def login_required(f): ...  # Decorator checking cookie auth_status == "authenticated"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create API helper module and update callback</name>
  <files>flask-example/api.py, flask-example/auth.py, flask-example/templates/base.html, flask-example/templates/callback_success.html</files>
  <read_first>
    - flask-example/auth.py
    - flask-example/cookies.py
    - flask-example/app.py
    - flask-example/templates/base.html
    - src/google_reviews_client/client.py
    - src/google_reviews_client/constants.py
    - src/google_reviews_client/models.py
    - src/google_reviews_client/exceptions.py
  </read_first>
  <action>
**1. Create `flask-example/api.py`** with two functions:

`get_client()` — reconstruct credentials from Fernet cookie and return GoogleReviewsClient (per D-06, D-07):
```python
from datetime import datetime
from flask import request, current_app, redirect
from google.oauth2.credentials import Credentials
from google_reviews_client.client import GoogleReviewsClient
from google_reviews_client.constants import BUSINESS_BASE
from google_reviews_client.models import Review
from cookies import decrypt_tokens, TOKEN_COOKIE_NAME

def get_client():
    """Reconstruct credentials from cookie and return a GoogleReviewsClient."""
    cookie = request.cookies.get(TOKEN_COOKIE_NAME)
    data = decrypt_tokens(cookie, current_app.config["FERNET"])
    if data is None or data.get("auth_status") != "authenticated":
        return None

    creds = Credentials(
        token=data["access_token"],
        refresh_token=data.get("refresh_token"),
        token_uri="https://oauth2.googleapis.com/token",
        client_id=current_app.config["GOOGLE_CLIENT_ID"],
        client_secret=current_app.config["GOOGLE_CLIENT_SECRET"],
        expiry=datetime.fromisoformat(data["expiry"]) if data.get("expiry") else None,
    )
    return GoogleReviewsClient(creds)
```

`get_reviews_page(client, location_name, page_token=None)` — single-page fetch returning `(reviews_list, next_page_token)` (per D-14, avoiding the auto-paginating generator):
```python
def get_reviews_page(client, location_name, page_token=None):
    """Fetch a single page of reviews, returning (reviews, next_page_token)."""
    url = f"{BUSINESS_BASE}/{location_name}/reviews"
    params = {}
    if page_token:
        params["pageToken"] = page_token
    data = client._authenticated_get(url, params=params)
    reviews = [Review.from_api_response(r) for r in data.get("reviews", [])]
    return reviews, data.get("nextPageToken")
```

**2. Update `flask-example/auth.py` callback route** (per D-02):
- Change the callback success response from `response = redirect("/accounts")` to `response = make_response(render_template("callback_success.html", show_logout=True))`
- Add `make_response, render_template` to the flask imports
- The cookie must still be set on this response (same `response.set_cookie(...)` call)

**3. Update `flask-example/templates/base.html`**:
- Add `{% block head_extra %}{% endblock %}` inside the `<head>` tag, after the Tailwind script tag
- This allows child templates to inject meta tags into the head

**4. Create `flask-example/templates/callback_success.html`** (per D-02):
```html
{% extends "base.html" %}
{% block title %}Authenticated - Google Reviews Demo{% endblock %}
{% block head_extra %}
<meta http-equiv="refresh" content="3;url=/">
{% endblock %}
{% block content %}
<div class="text-center py-12">
    <h1 class="text-3xl font-bold text-gray-900 mb-4">Authenticated successfully!</h1>
    <p class="text-gray-600">Redirecting to your accounts in a moment...</p>
</div>
{% endblock %}
```
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -c "from api import get_client, get_reviews_page; print('api.py imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - flask-example/api.py contains `def get_client():`
    - flask-example/api.py contains `def get_reviews_page(client, location_name, page_token=None):`
    - flask-example/api.py contains `from google.oauth2.credentials import Credentials`
    - flask-example/api.py contains `BUSINESS_BASE`
    - flask-example/auth.py callback route contains `render_template("callback_success.html"`
    - flask-example/auth.py does NOT contain `redirect("/accounts")`
    - flask-example/templates/base.html contains `{% block head_extra %}{% endblock %}`
    - flask-example/templates/callback_success.html contains `meta http-equiv="refresh" content="3;url=/"`
    - flask-example/templates/callback_success.html contains `Authenticated successfully!`
  </acceptance_criteria>
  <done>api.py module exists with get_client() and get_reviews_page(). Callback renders success template with meta refresh instead of redirecting. base.html has head_extra block.</done>
</task>

<task type="auto">
  <name>Task 2: Create template partials and all page templates</name>
  <files>flask-example/templates/_breadcrumb.html, flask-example/templates/_error.html, flask-example/templates/index.html, flask-example/templates/accounts.html, flask-example/templates/account.html, flask-example/templates/location.html, flask-example/templates/reviews.html</files>
  <read_first>
    - flask-example/templates/base.html
    - flask-example/templates/index.html
    - flask-example/templates/accounts.html
    - .planning/flask-demo/phases/03-data-browsing/03-UI-SPEC.md
    - .planning/flask-demo/phases/03-data-browsing/03-CONTEXT.md
  </read_first>
  <action>
**1. Create `flask-example/templates/_breadcrumb.html`** (per D-11, UX-05):
Receives `breadcrumbs` list of `{"label": str, "url": str}` dicts. Last item is current page (non-linked).
```html
{% if breadcrumbs %}
<nav aria-label="Breadcrumb" class="mb-6">
  <ol class="flex items-center space-x-2 text-sm text-gray-500">
    {% for crumb in breadcrumbs %}
    <li class="flex items-center">
      {% if not loop.last %}
      <a href="{{ crumb.url }}" class="hover:text-gray-700">{{ crumb.label }}</a>
      <span class="mx-2 text-gray-400" aria-hidden="true">&#8250;</span>
      {% else %}
      <span class="text-gray-900 font-medium">{{ crumb.label }}</span>
      {% endif %}
    </li>
    {% endfor %}
  </ol>
</nav>
{% endif %}
```

**2. Create `flask-example/templates/_error.html`** (per D-09, D-10):
Receives `error` dict with keys: `message` (str), `link` (str, optional), `link_text` (str, optional), `retry` (bool, optional).
```html
{% if error %}
<div role="alert" class="bg-red-50 border border-red-200 text-red-700 rounded-lg px-4 py-3 mb-6">
  {{ error.message }}
  {% if error.retry %}
  <a href="{{ request.url }}" class="underline font-medium">Try again</a>
  {% elif error.link %}
  <a href="{{ error.link }}" class="underline font-medium">{{ error.link_text }}</a>
  {% endif %}
</div>
{% endif %}
```

**3. Update `flask-example/templates/index.html`** (per D-01):
Keep the existing landing page content exactly as-is (error handling, title, trust statement, feature list, sign-in button). This template is ONLY rendered for unauthenticated users now (the view route handles conditional logic).

No changes needed to index.html -- it remains the landing page for unauthenticated users.

**4. Rewrite `flask-example/templates/accounts.html`** (per D-01, D-12, D-13, DATA-01, DATA-05):
Receives `accounts` (list of Account objects) and optionally `error` dict. Shows page heading "Your Accounts", includes error partial, lists accounts as clickable cards, or shows empty state.
```html
{% extends "base.html" %}
{% block title %}Your Accounts - Google Reviews Demo{% endblock %}
{% block content %}
{% include "_error.html" %}
<h1 class="text-2xl font-bold text-gray-900 mb-6">Your Accounts</h1>
{% if accounts %}
<div class="space-y-3">
  {% for account in accounts %}
  <a href="/account/{{ account.name.split('/')[-1] }}" class="block bg-white rounded-lg shadow-sm border border-gray-200 px-4 py-4 hover:shadow-md transition-shadow">
    <div class="text-lg font-bold text-gray-900">{{ account.account_name }}</div>
    {% if account.type %}
    <div class="text-sm text-gray-500">{{ account.type }}</div>
    {% endif %}
  </a>
  {% endfor %}
</div>
{% else %}
<div class="text-center py-12">
  <p class="text-gray-600 text-base">No accounts found. Make sure your Google account has access to a Business Profile.</p>
</div>
{% endif %}
{% endblock %}
```

**5. Create `flask-example/templates/account.html`** (per D-03, D-12, D-13, DATA-02, DATA-05, UX-05):
Receives `account` (Account), `locations` (list of Location), `breadcrumbs`, optionally `error`. Includes breadcrumb and error partials. Lists locations as clickable cards with title and store_code. Links to `/location/<location_id>?account=<account_id>`.
```html
{% extends "base.html" %}
{% block title %}{{ account.account_name }} - Google Reviews Demo{% endblock %}
{% block content %}
{% include "_breadcrumb.html" %}
{% include "_error.html" %}
<h1 class="text-2xl font-bold text-gray-900 mb-6">{{ account.account_name }}</h1>
{% if locations %}
<div class="space-y-3">
  {% for loc in locations %}
  <a href="/location/{{ loc.location_id }}?account={{ account.name.split('/')[-1] }}" class="block bg-white rounded-lg shadow-sm border border-gray-200 px-4 py-4 hover:shadow-md transition-shadow">
    <div class="text-lg font-bold text-gray-900">{{ loc.title or "Unnamed location" }}</div>
    {% if loc.store_code %}
    <div class="text-sm text-gray-500">{{ loc.store_code }}</div>
    {% endif %}
  </a>
  {% endfor %}
</div>
{% else %}
<div class="text-center py-12">
  <p class="text-gray-600 text-base">No locations found for this account.</p>
</div>
{% endif %}
{% endblock %}
```

**6. Create `flask-example/templates/location.html`** (per D-04, D-12, UX-05):
Receives `location` (Location), `account` (Account), `breadcrumbs`, optionally `error`. Shows location title, store_code, and a "View Reviews" button linking to `/location/<location_id>/reviews?account=<account_id>`.
```html
{% extends "base.html" %}
{% block title %}{{ location.title or "Location" }} - Google Reviews Demo{% endblock %}
{% block content %}
{% include "_breadcrumb.html" %}
{% include "_error.html" %}
<h1 class="text-2xl font-bold text-gray-900 mb-6">{{ location.title or "Unnamed location" }}</h1>
{% if location.store_code %}
<p class="text-sm text-gray-500 mb-6">Store code: {{ location.store_code }}</p>
{% endif %}
<a href="/location/{{ location.location_id }}/reviews?account={{ account.name.split('/')[-1] }}" class="inline-block bg-blue-600 text-white rounded-lg px-6 py-3 hover:bg-blue-700 focus:ring-2 focus:ring-blue-500 focus:ring-offset-2 font-medium">View Reviews</a>
{% endblock %}
```

**7. Create `flask-example/templates/reviews.html`** (per D-05, D-12, D-13, D-14, DATA-03, DATA-04, DATA-05, UX-05):
Receives `reviews` (list of Review), `location` (Location), `account` (Account), `breadcrumbs`, `next_page_token` (str or None), optionally `error`. Shows reviews as cards with star rating (N / 5 format), comment (truncated to 200 chars), author name, date (formatted as "Mar 15, 2025"), and reply if present. Pagination "Next page" link at bottom when next_page_token exists.
```html
{% extends "base.html" %}
{% block title %}Reviews for {{ location.title or "Location" }} - Google Reviews Demo{% endblock %}
{% block content %}
{% include "_breadcrumb.html" %}
{% include "_error.html" %}
<h1 class="text-2xl font-bold text-gray-900 mb-6">Reviews for {{ location.title or "Location" }}</h1>
{% if reviews %}
<div class="space-y-4">
  {% for review in reviews %}
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 px-4 py-4">
    <div class="flex items-center justify-between mb-2">
      <span class="text-sm text-gray-900 font-medium">{{ review.rating_value }} / 5</span>
      <span class="text-sm text-gray-500">{{ review.create_time.strftime('%b %d, %Y') }}</span>
    </div>
    {% if review.comment %}
    <p class="text-base text-gray-900 mb-2">{{ review.comment[:200] }}{% if review.comment|length > 200 %}...{% endif %}</p>
    {% endif %}
    <p class="text-sm text-gray-700">{{ review.reviewer.display_name }}</p>
    {% if review.has_reply %}
    <div class="ml-6 mt-3 pl-4 border-l-2 border-gray-200">
      <p class="text-xs text-gray-500 uppercase mb-1">Owner reply</p>
      <p class="text-sm text-gray-700">{{ review.review_reply.comment }}</p>
    </div>
    {% endif %}
  </div>
  {% endfor %}
</div>
{% if next_page_token %}
<div class="mt-6 flex justify-end">
  <a href="/location/{{ location.location_id }}/reviews?account={{ account.name.split('/')[-1] }}&page_token={{ next_page_token }}" class="text-blue-600 hover:text-blue-700 font-medium text-sm">Next page &rarr;</a>
</div>
{% endif %}
{% else %}
<div class="text-center py-12">
  <p class="text-gray-600 text-base">No reviews yet for this location.</p>
</div>
{% endif %}
{% endblock %}
```
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client && ls flask-example/templates/_breadcrumb.html flask-example/templates/_error.html flask-example/templates/accounts.html flask-example/templates/account.html flask-example/templates/location.html flask-example/templates/reviews.html && echo "All templates exist"</automated>
  </verify>
  <acceptance_criteria>
    - flask-example/templates/_breadcrumb.html contains `aria-label="Breadcrumb"`
    - flask-example/templates/_breadcrumb.html contains `aria-hidden="true"`
    - flask-example/templates/_breadcrumb.html contains `&#8250;`
    - flask-example/templates/_error.html contains `role="alert"`
    - flask-example/templates/_error.html contains `error.message`
    - flask-example/templates/_error.html contains `error.retry`
    - flask-example/templates/accounts.html contains `Your Accounts`
    - flask-example/templates/accounts.html contains `No accounts found`
    - flask-example/templates/accounts.html contains `account.account_name`
    - flask-example/templates/account.html contains `_breadcrumb.html`
    - flask-example/templates/account.html contains `No locations found for this account`
    - flask-example/templates/account.html contains `?account=`
    - flask-example/templates/location.html contains `View Reviews`
    - flask-example/templates/location.html contains `_breadcrumb.html`
    - flask-example/templates/reviews.html contains `/ 5`
    - flask-example/templates/reviews.html contains `Owner reply`
    - flask-example/templates/reviews.html contains `Next page`
    - flask-example/templates/reviews.html contains `next_page_token`
    - flask-example/templates/reviews.html contains `No reviews yet for this location`
    - flask-example/templates/reviews.html contains `%b %d, %Y`
  </acceptance_criteria>
  <done>All 7 templates exist with correct content: breadcrumb partial with accessibility attributes, error partial with role=alert, accounts list with empty state, account detail with locations, location detail with View Reviews button, reviews list with pagination and reply display.</done>
</task>

<task type="auto">
  <name>Task 3: Wire routes in views.py with error handling and breadcrumbs</name>
  <files>flask-example/views.py</files>
  <read_first>
    - flask-example/views.py
    - flask-example/api.py
    - flask-example/auth.py
    - flask-example/cookies.py
    - flask-example/templates/accounts.html
    - flask-example/templates/account.html
    - flask-example/templates/location.html
    - flask-example/templates/reviews.html
    - src/google_reviews_client/exceptions.py
    - src/google_reviews_client/models.py
    - src/google_reviews_client/client.py
  </read_first>
  <action>
Rewrite `flask-example/views.py` completely. Remove the old `/accounts` route. Implement these routes:

**Imports needed:**
```python
from flask import Blueprint, render_template, request, redirect
from auth import login_required
from api import get_client, get_reviews_page
from cookies import decrypt_tokens, TOKEN_COOKIE_NAME
from google_reviews_client.exceptions import (
    AuthenticationError, GooglePermissionError,
    RateLimitError, NotFoundError, GoogleAPIError,
)
```

**Helper function for error context** (per D-10 — exact copy text from UI-SPEC.md):
```python
def _error_context(exc, back_url="/", back_text="Back to accounts"):
    """Map library exception to error dict for template rendering."""
    if isinstance(exc, GooglePermissionError):
        return {"message": "You don't have permission to access this resource.", "link": "/", "link_text": "Back to accounts"}
    if isinstance(exc, RateLimitError):
        return {"message": "Too many requests. Please try again in a few moments.", "retry": True}
    if isinstance(exc, NotFoundError):
        return {"message": "Resource not found.", "link": "/", "link_text": "Back to accounts"}
    # GoogleAPIError (5xx) or any other
    return {"message": "Google is temporarily unavailable. Please try again later.", "retry": True}
```

**Route 1: `GET /`** (per D-01, DATA-01, DATA-05):
- Check for authenticated cookie (read TOKEN_COOKIE_NAME, call decrypt_tokens, check auth_status)
- If authenticated: call `get_client()`, then `client.list_accounts()`, render `accounts.html` with `accounts=accounts, show_logout=True`
- If AuthenticationError: `redirect("/login")`
- If other API error: render `accounts.html` with `error=_error_context(exc), accounts=[], show_logout=True`
- If not authenticated: render `index.html` with `error=request.args.get("error")`

**Route 2: `GET /account/<account_id>`** (per D-03, DATA-02, DATA-05, UX-05):
- Decorated with `@login_required`
- Construct account resource name: `f"accounts/{account_id}"`
- Call `client.list_accounts()` to find the account object for breadcrumb label (filter by `acc.name == account_name`)
- Call `client.list_locations(account_name)`
- Build breadcrumbs: `[{"label": "Accounts", "url": "/"}]` (current page is account name, rendered by template as last non-linked segment via the `account.html` heading)
- Actually, breadcrumbs list should include the current page as last item: `[{"label": "Accounts", "url": "/"}, {"label": account.account_name, "url": ""}]`
- Render `account.html` with `account=account, locations=locations, breadcrumbs=breadcrumbs, show_logout=True`
- If account not found in list_accounts, use a fallback Account with `account_name=account_id`
- Error handling: AuthenticationError -> redirect("/login"), others -> render account.html with error

**Route 3: `GET /location/<location_id>`** (per D-04, UX-05):
- Decorated with `@login_required`
- Read `account_id` from `request.args.get("account")`
- If no account_id query param, redirect to `/` (can't fetch location without account context)
- Construct account name `f"accounts/{account_id}"`, call `client.list_locations(account_name)`
- Find the matching location by `loc.location_id == location_id`
- Get account for breadcrumb: call `client.list_accounts()`, find matching account
- Build breadcrumbs: `[{"label": "Accounts", "url": "/"}, {"label": account.account_name, "url": f"/account/{account_id}"}, {"label": location.title or "Location", "url": ""}]`
- Render `location.html` with `location=location, account=account, breadcrumbs=breadcrumbs, show_logout=True`
- Error handling: same pattern

**Route 4: `GET /location/<location_id>/reviews`** (per D-05, D-14, DATA-03, DATA-04, DATA-05, UX-05):
- Decorated with `@login_required`
- Read `account_id` from `request.args.get("account")`, `page_token` from `request.args.get("page_token")`
- If no account_id, redirect to `/`
- Construct full location resource name: `f"accounts/{account_id}/locations/{location_id}"`
- Call `get_reviews_page(client, location_name, page_token)`
- Get location and account objects for breadcrumbs (list_locations, list_accounts)
- Build breadcrumbs: `[{"label": "Accounts", "url": "/"}, {"label": account.account_name, "url": f"/account/{account_id}"}, {"label": location.title or "Location", "url": f"/location/{location_id}?account={account_id}"}, {"label": "Reviews", "url": ""}]`
- Render `reviews.html` with `reviews=reviews, location=location, account=account, breadcrumbs=breadcrumbs, next_page_token=next_token, show_logout=True`
- Error handling: same pattern

**Performance note for location/reviews routes:** The route makes multiple API calls (list_accounts + list_locations + the main data call). This is acceptable for a demo app. An optimization would be to pass names through query params, but keeping API lookups ensures breadcrumb labels are always accurate.

**Simplification option:** To avoid extra API calls for breadcrumb labels, pass `account_name` and `location_title` as query params from the linking pages. The executor may choose either approach. If using query params, validate they exist and fall back to API calls if missing.
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -c "from views import views_bp; rules = [r.rule for r in views_bp.deferred_functions.__closure__[0].cell_contents.url_map.iter_rules() if 'views' in str(r.endpoint)] if hasattr(views_bp, 'deferred_functions') else []; print('views.py imports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - flask-example/views.py contains `from api import get_client, get_reviews_page`
    - flask-example/views.py contains `from google_reviews_client.exceptions import`
    - flask-example/views.py contains `def index():`
    - flask-example/views.py contains `@views_bp.route("/account/<account_id>")`
    - flask-example/views.py contains `@views_bp.route("/location/<location_id>")`
    - flask-example/views.py contains `@views_bp.route("/location/<location_id>/reviews")`
    - flask-example/views.py contains `def _error_context(`
    - flask-example/views.py contains `except AuthenticationError`
    - flask-example/views.py contains `redirect("/login")`
    - flask-example/views.py contains `breadcrumbs`
    - flask-example/views.py contains `get_reviews_page(client`
    - flask-example/views.py contains `request.args.get("account")`
    - flask-example/views.py contains `request.args.get("page_token")`
    - flask-example/views.py does NOT contain `@views_bp.route("/accounts")`
  </acceptance_criteria>
  <done>views.py has 4 routes: conditional / (landing vs accounts), /account/<id> with locations, /location/<id> with details, /location/<id>/reviews with pagination. All routes have error handling with AuthenticationError redirect and inline banners for other errors. Breadcrumbs passed to all data page templates.</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `cd /Users/caue/dev/google-reviews-client/flask-example && python -c "from app import create_app; app = create_app(); print('App creates OK')"` (requires env vars)
2. Verify all template files exist and contain expected patterns
3. Verify views.py has no syntax errors: `python -c "import views"`
4. Run existing test suite (some Phase 2 tests will break due to callback change and /accounts removal -- that is expected and will be fixed in Plan 02)
</verification>

<success_criteria>
- api.py exists with get_client() and get_reviews_page()
- auth.py callback renders success template with meta refresh (not redirect to /accounts)
- base.html has head_extra block
- 6 new/updated templates exist with correct Tailwind styling per UI-SPEC.md
- views.py has 4 routes with full error handling, breadcrumbs, and empty states
- No /accounts route remains (replaced by conditional /)
- All Python files have no syntax errors
</success_criteria>

<output>
After completion, create `.planning/flask-demo/phases/03-data-browsing/03-01-SUMMARY.md`
</output>
