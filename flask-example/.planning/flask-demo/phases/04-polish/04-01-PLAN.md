---
phase: 04-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api.py
  - views.py
  - templates/reviews.html
autonomous: true
requirements: [UX-03, UX-04, UX-06, UX-07]

must_haves:
  truths:
    - "Star ratings display as Unicode stars (filled + empty) instead of 'N / 5' text"
    - "Reviewer photos appear as 40px circles next to each review"
    - "Anonymous or missing-photo reviewers show a gray circle with first initial"
    - "Summary bar above reviews shows average rating number, star display, and total count"
    - "Code snippet section at bottom shows pip install + library usage with current location name"
    - "Copy button copies code snippet text to clipboard"
  artifacts:
    - path: "api.py"
      provides: "Extended get_reviews_page returning totalReviewCount and averageRating"
      contains: "totalReviewCount"
    - path: "views.py"
      provides: "Reviews route passing total_review_count and average_rating to template"
      contains: "total_review_count"
    - path: "templates/reviews.html"
      provides: "Full polish: stars, photos, summary bar, code snippet with copy button"
      contains: "aria-label"
  key_links:
    - from: "api.py"
      to: "views.py"
      via: "get_reviews_page return value unpacking"
      pattern: "total_review_count.*average_rating"
    - from: "views.py"
      to: "templates/reviews.html"
      via: "render_template context variables"
      pattern: "total_review_count=.*average_rating="
    - from: "templates/reviews.html"
      to: "navigator.clipboard"
      via: "Copy button onclick handler"
      pattern: "navigator\\.clipboard\\.writeText"
---

<objective>
Add visual polish to the reviews page: Unicode star ratings, reviewer photos with fallback, review summary bar, and library code snippet with copy button.

Purpose: Transform the reviews page from a plain data dump into a visually complete demo that clearly communicates the library's value.
Output: Updated api.py, views.py, and reviews.html with all four UX polish features.
</objective>

<execution_context>
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/flask-demo/ROADMAP.md
@.planning/flask-demo/phases/04-polish/04-UI-SPEC.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/google_reviews_client/models.py:
```python
class Reviewer:
    display_name: str
    profile_photo_url: str | None = None
    is_anonymous: bool = False

class Review:
    reviewer: Reviewer
    star_rating: StarRating
    comment: str
    create_time: datetime
    review_reply: ReviewReply | None = None

    @property
    def rating_value(self) -> int:  # Returns 1-5
    @property
    def has_reply(self) -> bool:
```

From api.py (current):
```python
def get_reviews_page(client, location_name, page_token=None):
    """Fetch a single page of reviews, returning (reviews, next_page_token)."""
    # Returns: (list[Review], str|None)
```

From views.py (current reviews route):
```python
review_list, next_token = get_reviews_page(client, location_name, page_token)
# Template receives: reviews, location, account, breadcrumbs, next_page_token, show_logout
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API helper and reviews route to pass summary data</name>
  <files>api.py, views.py</files>
  <read_first>api.py, views.py, src/google_reviews_client/models.py</read_first>
  <action>
1. In api.py, update get_reviews_page() to return 4 values instead of 2. After the existing `data = client._authenticated_get(url, params=params)` line, extract totalReviewCount and averageRating from `data`. Return signature becomes:
   ```python
   return reviews, data.get("nextPageToken"), data.get("totalReviewCount"), data.get("averageRating")
   ```

2. In views.py, update the reviews() route to unpack 4 values:
   ```python
   review_list, next_token, total_review_count, average_rating = get_reviews_page(client, location_name, page_token)
   ```

3. In views.py, pass the new values to render_template in the reviews() route. Add `total_review_count=total_review_count, average_rating=average_rating` to both the success and error render_template calls. For the error case, pass `total_review_count=None, average_rating=None`.

4. Also compute `avg_rounded` for the star display: `avg_rounded=round(average_rating) if average_rating else 0` and pass it to the template.
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -c "from api import get_reviews_page; from views import views_bp; print('imports ok')"</automated>
  </verify>
  <acceptance_criteria>
    - api.py get_reviews_page returns 4 values: reviews, nextPageToken, totalReviewCount, averageRating
    - views.py reviews() unpacks 4 values and passes total_review_count, average_rating, avg_rounded to template
    - Error case passes total_review_count=None, average_rating=None, avg_rounded=0
    - grep -c "totalReviewCount" api.py returns 1
    - grep -c "total_review_count" views.py returns at least 3
    - grep -c "average_rating" views.py returns at least 3
  </acceptance_criteria>
  <done>get_reviews_page returns summary data; reviews route passes it to template context</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild reviews template with stars, photos, summary bar, snippet, and copy button</name>
  <files>templates/reviews.html</files>
  <read_first>templates/reviews.html, templates/base.html, .planning/flask-demo/phases/04-polish/04-UI-SPEC.md</read_first>
  <action>
Rewrite templates/reviews.html with these sections, following the UI-SPEC exactly:

**1. Review Summary Bar (UX-06)** — Insert between the h1 heading and the review list. Only render when total_review_count is not None:
```html
{% if total_review_count is not none %}
<div class="bg-white rounded-lg shadow-sm border border-gray-200 px-4 py-4 mb-6 flex items-center gap-6">
  <div>
    <span class="text-xl font-bold text-gray-900">{{ average_rating }}</span>
    <span class="text-amber-500 text-xl tracking-wide ml-1">{{ "\u2605" * avg_rounded }}{{ "\u2606" * (5 - avg_rounded) }}</span>
  </div>
  <div class="text-sm text-gray-500">
    {{ total_review_count }} review{{ "s" if total_review_count != 1 }}
  </div>
</div>
{% endif %}
```

**2. Review Cards — restructure each card** per the UI-SPEC layout:
- Wrap card content in a flex row: photo on left, content on right with gap-4
- Photo: If review.reviewer.is_anonymous is false and review.reviewer.profile_photo_url is not none, show `<img>` with `w-10 h-10 rounded-full object-cover flex-shrink-0 loading="lazy" referrerpolicy="no-referrer"`. Add onerror handler: `onerror="this.style.display='none';this.nextElementSibling.style.display='flex'"`. Immediately after img, place hidden fallback div.
- Fallback (anonymous or no photo): `<div class="w-10 h-10 rounded-full bg-gray-200 flex items-center justify-center flex-shrink-0" aria-hidden="true"><span class="text-gray-500 text-sm font-medium">{{ (review.reviewer.display_name[0] | upper) if review.reviewer.display_name else "A" }}</span></div>`
- For the case where both img and fallback are rendered (non-anonymous with photo), the fallback div must have `style="display:none"` initially and class `hidden` removed — use `style="display:none"` and the onerror toggles it to `display:flex`.

**3. Star Rating (UX-03)** — Replace `{{ review.rating_value }} / 5` with:
```html
<span class="text-amber-500 tracking-wide" aria-label="{{ review.rating_value }} out of 5 stars">{{ "\u2605" * review.rating_value }}{{ "\u2606" * (5 - review.rating_value) }}</span>
```
Keep the date on the right side of the same flex row.

**4. Library Code Snippet (UX-07)** — Add at the bottom of the page, after the pagination link, outside the reviews conditional block (always shown when reviews exist). Per D-11 from CONTEXT.md, include a Copy button using navigator.clipboard.writeText():
```html
{% if reviews %}
<div class="mt-8 pt-8 border-t border-gray-200">
  <h2 class="text-lg font-bold text-gray-900 mb-2">Get this data programmatically</h2>
  <p class="text-sm text-gray-600 mb-4">Use the google-reviews-client library to access these reviews in your own code:</p>
  <div class="relative">
    <button onclick="navigator.clipboard.writeText(document.getElementById('code-snippet').textContent).then(()=>{this.textContent='Copied!';setTimeout(()=>this.textContent='Copy',2000)})" class="absolute top-2 right-2 bg-white border border-gray-300 rounded px-2 py-1 text-xs text-gray-600 hover:bg-gray-50">Copy</button>
    <pre class="bg-gray-100 border border-gray-200 rounded-lg px-4 py-4 text-sm font-mono text-gray-900 overflow-x-auto"><code id="code-snippet">pip install google-reviews-client

from google_reviews_client import GoogleReviewsClient

client = GoogleReviewsClient(credentials)
for review in client.list_reviews("{{ location.name }}"):
    print(review.reviewer.display_name, review.star_rating, review.comment)</code></pre>
  </div>
</div>
{% endif %}
```

**5. Preserve existing behavior:** breadcrumb include, error include, h1 heading, pagination link, empty state message ("No reviews yet for this location.").

Key Tailwind classes from UI-SPEC:
- Star filled: text-amber-500
- Star empty: text-gray-300 (but using Unicode WHITE STAR in amber-500 tracking-wide is the spec pattern — the empty stars are rendered as part of the same span)
- Photo: w-10 h-10 rounded-full
- Summary card: bg-white rounded-lg shadow-sm border border-gray-200 px-4 py-4
- Code block: bg-gray-100 border border-gray-200 rounded-lg px-4 py-4 text-sm font-mono
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -c "
from app import create_app
app = create_app()
with app.app_context():
    from flask import render_template
    # Will fail on undefined vars but confirms template syntax is valid
    try:
        app.jinja_env.get_template('reviews.html')
        print('template parses ok')
    except Exception as e:
        print(f'FAIL: {e}')
"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "aria-label" templates/reviews.html returns at least 1 (star accessibility)
    - grep -c "rounded-full" templates/reviews.html returns at least 2 (photo circles)
    - grep -c "total_review_count" templates/reviews.html returns at least 1 (summary bar)
    - grep -c "average_rating" templates/reviews.html returns at least 1 (summary bar)
    - grep -c "code-snippet" templates/reviews.html returns at least 2 (code block id + clipboard ref)
    - grep -c "navigator.clipboard.writeText" templates/reviews.html returns 1 (copy button per D-11)
    - grep -c "referrerpolicy" templates/reviews.html returns at least 1 (Google photo policy)
    - grep -c "onerror" templates/reviews.html returns at least 1 (broken image fallback)
    - Template file parses without Jinja2 syntax errors
  </acceptance_criteria>
  <done>Reviews page displays star ratings as Unicode stars, reviewer photos with fallback, summary bar with average and count, and code snippet with copy button</done>
</task>

</tasks>

<verification>
1. `python -c "from api import get_reviews_page; from views import views_bp"` — imports succeed
2. `grep "totalReviewCount" api.py` — field extracted from API response
3. `grep "total_review_count" views.py` — passed to template
4. `grep "aria-label" templates/reviews.html` — accessible star ratings
5. `grep "navigator.clipboard" templates/reviews.html` — copy button present
6. `grep "rounded-full" templates/reviews.html` — photo circles present
7. Template parses without Jinja2 errors
</verification>

<success_criteria>
- Star ratings show as filled/empty Unicode stars with aria-label accessibility
- Reviewer photos display as 40px circles with lazy loading and referrerpolicy
- Anonymous reviewers show gray initial-letter fallback; broken images fall back via onerror
- Summary bar shows average rating (number + stars) and total review count
- Code snippet section with "Get this data programmatically" heading appears below reviews
- Copy button uses navigator.clipboard.writeText() per D-11
- All existing functionality preserved (breadcrumbs, pagination, error handling, empty state)
</success_criteria>

<output>
After completion, create `.planning/flask-demo/phases/04-polish/04-01-SUMMARY.md`
</output>
