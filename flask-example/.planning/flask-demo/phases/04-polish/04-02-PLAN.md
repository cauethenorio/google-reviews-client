---
phase: 04-polish
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_views.py
  - tests/test_api.py
autonomous: true
requirements: [UX-03, UX-04, UX-06, UX-07]

must_haves:
  truths:
    - "Tests verify star rating HTML contains Unicode stars and aria-label"
    - "Tests verify reviewer photo img tag with referrerpolicy and fallback div"
    - "Tests verify summary bar renders with average rating and total count"
    - "Tests verify code snippet section with copy button appears on reviews page"
    - "Tests verify get_reviews_page returns 4-tuple with totalReviewCount and averageRating"
  artifacts:
    - path: "tests/test_views.py"
      provides: "Tests for all four polish features on reviews page"
      contains: "aria-label"
    - path: "tests/test_api.py"
      provides: "Test for extended get_reviews_page return value"
      contains: "totalReviewCount"
  key_links:
    - from: "tests/test_views.py"
      to: "views.py"
      via: "Flask test client GET /location/loc1/reviews"
      pattern: "client.get.*reviews"
    - from: "tests/test_api.py"
      to: "api.py"
      via: "Mock _authenticated_get to return totalReviewCount/averageRating"
      pattern: "totalReviewCount.*averageRating"
---

<objective>
Add tests for all Phase 4 polish features: star ratings, reviewer photos, summary bar, code snippet with copy button, and the extended API helper.

Purpose: Verify the visual polish features are correctly rendered and the API data pipeline works.
Output: Extended test_views.py and test_api.py with tests covering UX-03, UX-04, UX-06, UX-07.
</objective>

<execution_context>
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/workflows/execute-plan.md
@/Users/caue/dev/google-reviews-client/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/flask-demo/ROADMAP.md
@.planning/flask-demo/phases/04-polish/04-01-SUMMARY.md

<interfaces>
<!-- Executor needs to understand existing test patterns -->

From tests/test_views.py (pattern):
```python
# Tests use pytest fixtures: client (Flask test client), auth_cookie (encrypted cookie)
# Pattern: client.get("/path", headers={"Cookie": f"gbp_token={auth_cookie}"})
# Assertions: assert b"expected text" in response.data
```

From api.py (after plan 01):
```python
def get_reviews_page(client, location_name, page_token=None):
    # Returns: (list[Review], str|None, int|None, float|None)
    # (reviews, nextPageToken, totalReviewCount, averageRating)
```

From views.py (after plan 01):
```python
# reviews() route passes to template:
# total_review_count, average_rating, avg_rounded
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add test for extended get_reviews_page return value</name>
  <files>tests/test_api.py</files>
  <read_first>tests/test_api.py, api.py</read_first>
  <action>
Add a new test class or test function in tests/test_api.py that verifies get_reviews_page returns 4 values including totalReviewCount and averageRating:

1. Create a test that mocks `client._authenticated_get` to return a dict with:
   ```python
   {"reviews": [...], "nextPageToken": "token123", "totalReviewCount": 42, "averageRating": 4.3}
   ```
2. Assert the function returns a 4-tuple: `(reviews, "token123", 42, 4.3)`
3. Create a test with missing totalReviewCount/averageRating fields — assert they return as None:
   ```python
   {"reviews": [...]}
   ```
   Returns `(reviews, None, None, None)`

Follow the existing test patterns in test_api.py (mock style, fixture usage).
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -m pytest tests/test_api.py -x -v 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "totalReviewCount" tests/test_api.py returns at least 1
    - grep -c "averageRating" tests/test_api.py returns at least 1
    - All tests in test_api.py pass
  </acceptance_criteria>
  <done>get_reviews_page tested for 4-value return with and without summary fields</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for polish features on reviews page</name>
  <files>tests/test_views.py</files>
  <read_first>tests/test_views.py, templates/reviews.html, views.py</read_first>
  <action>
Add a new test class `TestReviewsPolish` (or extend existing reviews tests) in tests/test_views.py. Mock the API calls to return review data with known values. Tests needed:

**UX-03 Star Ratings:**
1. Test that reviews page contains Unicode filled star character (U+2605) instead of "N / 5" text
2. Test that reviews page contains `aria-label` with "out of 5 stars" text
3. Verify the old "/ 5" pattern is NOT present: `assert b"/ 5" not in response.data`

**UX-04 Reviewer Photos:**
4. Test that reviews page contains `<img` tag with `referrerpolicy="no-referrer"` when reviewer has photo URL
5. Test that reviews page contains fallback div with `rounded-full bg-gray-200` for anonymous reviewer
6. Test onerror attribute present on img tags

**UX-06 Summary Bar:**
7. Test that reviews page shows the average rating number (e.g., "4.3") when totalReviewCount is present
8. Test that reviews page shows review count text (e.g., "42 reviews")
9. Test that summary bar is NOT rendered when totalReviewCount is None

**UX-07 Code Snippet:**
10. Test that reviews page contains "Get this data programmatically" heading
11. Test that reviews page contains `pip install google-reviews-client` text
12. Test that reviews page contains `navigator.clipboard.writeText` (copy button per D-11)
13. Test that the location name appears in the code snippet

Mock setup: Mock `get_client` to return a mock client. Mock `client.list_accounts()` to return an account. Mock `client.list_locations()` to return a location. Mock `get_reviews_page` to return `(reviews, None, 42, 4.3)` — a list of reviews with known reviewer data (one with photo URL, one anonymous).

Use the existing test patterns — authenticated requests use the auth_cookie fixture in Cookie header.
  </action>
  <verify>
    <automated>cd /Users/caue/dev/google-reviews-client/flask-example && python -m pytest tests/test_views.py -x -v -k "polish or Polish or star or photo or snippet or summary" 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "aria-label" tests/test_views.py returns at least 1
    - grep -c "navigator.clipboard" tests/test_views.py returns at least 1
    - grep -c "referrerpolicy" tests/test_views.py returns at least 1
    - grep -c "Get this data programmatically" tests/test_views.py returns at least 1
    - All new tests pass
    - All existing tests still pass: python -m pytest tests/ -x
  </acceptance_criteria>
  <done>All four polish features have test coverage; no regressions in existing tests</done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_api.py -x -v` — all API tests pass including new summary data tests
2. `python -m pytest tests/test_views.py -x -v` — all view tests pass including new polish tests
3. `python -m pytest tests/ -x` — full test suite passes with no regressions
</verification>

<success_criteria>
- test_api.py has tests for 4-value get_reviews_page return
- test_views.py has tests for star ratings (Unicode + aria-label), photos (img + fallback), summary bar (average + count + conditional), and code snippet (heading + pip install + copy button + location name)
- Full test suite passes with zero failures
</success_criteria>

<output>
After completion, create `.planning/flask-demo/phases/04-polish/04-02-SUMMARY.md`
</output>
